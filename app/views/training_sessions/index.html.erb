<%= month_calendar events: @trainingsessions do |date, trainingsession| %>
  <%= date %>

  <% trainingsessions.each do |trainingsession| %>
    <div>
      <%= trainingsession.name %>
    </div>
  <% end %>
<% end %>
