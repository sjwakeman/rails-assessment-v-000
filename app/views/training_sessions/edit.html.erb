<h2>Edit Training Session Page</h2>
<%= form_for @training_session do |f| %>
  <% if @training_session.errors.any? %>
    <h2>Errors</h2>
    <ul>
      <% @training_session.full_messages.each do |message| %>
        <li><%= message %></li>
    </ul>
      <% end %>
  <% end %>
  <table>
    <tr><td><%= f.label :date %> :
    <%= f.text_field :date %></td></tr>
    <tr><td> </td></tr>

    <tr><td>Client : <%= f.text_field :client_name, list: "client_name_autocomplete" %>
      <datalist id="client_name_autocomplete">
        <% Client.all.each do |client| %>
          <option value="<%= client.name %>">
        <% end %>
      </datalist></td></tr>
    <tr><td> </td></tr>

    <tr><td><%= f.label :start_time %> :
    <%= f.text_field :start_time, value: @training_session.start_time.strftime("%l:%M %p") %></td></tr>
    <tr><td> </td></tr>
    <tr><td><%= f.label :end_time %> :
    <%= f.text_field :end_time, value: @training_session.start_time.strftime("%l:%M %p") %></td></tr>
    <tr><td> </td></tr>

    <tr><td><%= f.label :location %> :
    <%= f.text_field :location %></td></tr>
    <tr><td> </td></tr>

    <tr><td><%= f.label :booked_status %> :
    <%= f.text_field :booked_status %></td></tr>
    <tr><td> </td></tr>

    <tr><td><%= f.submit %></td></tr>
    <tr><td> </td></tr>
  </table>
<% end %>
